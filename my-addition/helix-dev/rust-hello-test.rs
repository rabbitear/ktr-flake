
Here's a simple "Hello, World!" program in Rust:

```rust
fn main() {
    println!("Hello, World!");
}
```

To run this program:
1. Save it in a file named `main.rs`
2. Compile it with `rustc main.rs`
3. Run the executable with `./main` (on Linux/macOS) or `main.exe` (on Windows)

The `println!` macro is Rust's way of printing text to the console. The `!` indicates it's a macro rather than a regular function. The `main` function is the entry point of every Rust program.
