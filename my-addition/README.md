# Custom Additions

Custom packages, modules, and scripts developed for this flake.

- `packages/`: Custom Nix package derivations
- `modules/`: Home-manager modules for user-specific features
- `scripts/`: Utility scripts and script collections