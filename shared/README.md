# Shared Modules

Common NixOS modules and configurations shared across multiple hosts.

- `default.nix`: Core system configuration (desktop, networking, etc.)
- `packages/`: System packages organized by category
- `services/`: Service configurations (virtualization, web services)
- `system/`: System-wide settings (fonts, security)
